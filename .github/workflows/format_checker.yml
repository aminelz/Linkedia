name: Format Check

on: [pull_request]

jobs:
  black:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: psf/black@stable
      - uses: isort/isort-action@v1
        with:
            options: "--line-length=120 ----profile black"